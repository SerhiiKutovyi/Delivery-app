import { BoxLayout } from './Layout.styled';
import AppBar from 'components/AppBar';

const Layout = () => {
  return (
    <BoxLayout>
      <AppBar />
    </BoxLayout>
  );
};

export default Layout;
